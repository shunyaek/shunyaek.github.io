import { Footer as MantineFooter } from '@mantine/core';

function Footer() {
  return (
    <MantineFooter height={50} p="sm">
      Application footer
    </MantineFooter>
  );
}

export default Footer;